__pycache__/
*.pyc
.venv/
venv/
.DS_Store
.streamlit/secrets.toml
kinematik_setup.json
kinematik_sweep.csv

# --- Tenant / user data: never commit -------------------------------------
# LocalWorkspaceBackend writes <root>/workspaces/<workspace_id>/project.json.
# On a laptop that is real team data; it must not reach the public repo.
workspaces/

# Usage telemetry. Contains session ids and timestamps for real users at UK,
# EU, Canadian, Australian and Singaporean universities — personal data under
# UK/EU GDPR and Singapore's PDPA. Keep it out of a public repository.
analytics_buffer.jsonl
analytics_buffer*.jsonl

# Local credential files (belt and braces alongside .streamlit/secrets.toml)
.env
.env.*
*.key
service_role*.txt
